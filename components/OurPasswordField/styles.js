import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	mainContainer: {
		position: "relative",
	},
	eye: {
		position: "absolute",
		right: 4,
		bottom: 24,
	}
});

export default styles;
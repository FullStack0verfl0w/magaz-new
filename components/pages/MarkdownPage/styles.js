import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	scrollViewContainer: {
		padding: 20,
		paddingRight: 8,
	},
	background: {
		position: "absolute",
		left: 0,
		right: 0,
		top: 0,
		bottom: 0,
		minHeight: "100%",
	},
});

export default styles;